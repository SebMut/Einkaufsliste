import fs from 'node:fs/promises';
import path from 'node:path';
import os from 'node:os';
import { execFile } from 'node:child_process';
import { promisify } from 'node:util';
import * as cheerio from 'cheerio';

const execFileAsync=promisify(execFile);
const ROOT=path.resolve(process.cwd(),'..');
const LIVE_FILE=path.join(ROOT,'data','offers-live.json');
const PREVIEW_FILE=path.join(ROOT,'data','metro-prospekt-preview.json');
const PREVIEW=process.env.METRO_PREVIEW==='1';
const now=new Date().toISOString();

const FOOD=/rind|kalb|schwein|hähn|pute|lamm|ente|fleisch|steak|roastbeef|entrec|filet|schnitzel|wurst|salami|schinken|fisch|lachs|thunfisch|garnelen|forelle|dorade|milch|butter|joghurt|quark|käse|sahne|eier|zwetsch|pflaum|nektarin|feigen|apfel|äpfel|banane|tomat|paprika|gurke|kartoff|zwiebel|möhre|beeren|trauben|avocado|pommes|fries|pizza|brot|brötchen|semmel|baguette|nudel|pasta|reis|mehl|zucker|öl|kaffee|espresso|tee|wasser|saft|cola|limonade|bier|wein|sekt|schokolade|keks|chips|snack|eis|hummus|frucht|gemüse|obst/i;
const REJECT=/weitere infos|metro\.de|gültigkeitszeitraum|gewerbetreib|wiederverkauf|verfügbarkeit|preise ohne|mehrwertsteuer|frische\s*&?\s*basics|getränke\s*&?\s*snacks|nonfood|hygiene|bild ki|klasse i|herkunft|vorgereift|vak\.?-?verp|zertifiziert|qualität|tiefpreis|angebot|abgabe nur|deutschland gmbh/i;
const ICONS={'Obst & Gemüse':'🥦','Milchprodukte':'🥛','Fleisch & Fisch':'🥩','Kaffee & Frühstück':'☕','Vorrat':'🍝','Tiefkühl':'🧊','Getränke':'🥤','Süßes & Snacks':'🍫','Backwaren':'🥖','Lebensmittel':'🛒'};

function pad(n){return String(n).padStart(2,'0')}
function weekInfo(date=new Date()){const d=new Date(Date.UTC(date.getUTCFullYear(),date.getUTCMonth(),date.getUTCDate()));const monday=new Date(d);monday.setUTCDate(d.getUTCDate()-((d.getUTCDay()+6)%7));const saturday=new Date(monday);saturday.setUTCDate(monday.getUTCDate()+5);const f=x=>`${pad(x.getUTCDate())}${pad(x.getUTCMonth()+1)}${String(x.getUTCFullYear()).slice(-2)}`;return{slug:`wochen-angebote-${f(monday)}-${f(saturday)}`,validFrom:monday.toISOString().slice(0,10),validTo:saturday.toISOString().slice(0,10)}}
function clean(s=''){return String(s).replace(/[\u00ad\uFEFF]/g,'').replace(/\s+/g,' ').trim()}
function num(s){return Number(String(s).replace('.','').replace(',','.').replace(/[^0-9.]/g,''))}
function decodeJsString(s){return s.replace(/\\u0026/g,'&').replace(/\\u0027/g,"'").replace(/\\\//g,'/').replace(/&amp;/g,'&')}
function category(t){if(/zwetsch|pflaum|nektarin|feigen|apfel|äpfel|banane|tomat|paprika|gurke|kartoff|zwiebel|möhre|beeren|trauben|avocado|obst|gemüse/i.test(t))return'Obst & Gemüse';if(/milch|butter|joghurt|quark|käse|sahne/i.test(t))return'Milchprodukte';if(/rind|kalb|schwein|hähn|pute|lamm|ente|fleisch|steak|roastbeef|entrec|filet|schnitzel|wurst|salami|schinken|fisch|lachs|thunfisch|garnelen|forelle|dorade/i.test(t))return'Fleisch & Fisch';if(/kaffee|espresso|tee|müsli|eier/i.test(t))return'Kaffee & Frühstück';if(/nudel|pasta|reis|mehl|zucker|öl|hummus/i.test(t))return'Vorrat';if(/pommes|fries|tiefkühl|pizza|eiscreme|speiseeis/i.test(t))return'Tiefkühl';if(/wasser|saft|cola|limonade|bier|wein|sekt/i.test(t))return'Getränke';if(/schokolade|keks|chips|snack|bonbon/i.test(t))return'Süßes & Snacks';if(/brot|brötchen|semmel|baguette/i.test(t))return'Backwaren';return'Lebensmittel'}
function titleFromBlock(text=''){let t=clean(text);t=t.split('•')[0].trim();t=t.replace(/\b(?:TIEFPREIS|ANGEBOT)\b.*$/i,'').trim();t=t.replace(/\s+-\s+/g,'-').replace(/-\s+/g,'-').trim();if(t.length<3||t.length>120||!/[A-Za-zÄÖÜäöüß]{3}/.test(t))return'';if(REJECT.test(t)||/^\d|^(?:ab|ca\.?|je|pro|stück|stücke|packung|karton|beutel|schale|sack|säcke)\b/i.test(t))return'';if(/\d+[,.]\d{2}\*?/.test(t))return'';if(!FOOD.test(t))return'';return t}
function categoryIcon(c){return ICONS[c]||'🛒'}
function blockDistance(price,b){const pcx=(price.x1+price.x2)/2,bcx=(b.x1+b.x2)/2;const dy=Math.max(0,price.y1-b.y2);const dx=Math.abs((pcx-105)-bcx);return dy*1.4+dx*.35}
function unitFor(price,blocks){const near=blocks.filter(b=>b.i!==price.i&&b.y1>=price.y1-18&&b.y2<=price.y2+26&&b.x1>=price.x1-45&&b.x2<=price.x2+45).map(b=>clean(b.text)).join(' ');if(/je\s*kg/i.test(`${price.text} ${near}`))return'€/kg';if(/je\s*(?:l|Liter)\b/i.test(`${price.text} ${near}`))return'€/l';return'€/Packung'}
function conditionFor(price,blocks){const c=blocks.filter(b=>b.i!==price.i&&b.y1>=price.y1-20&&b.y2<=price.y2+26&&b.x1>=price.x1-85&&b.x2<=price.x1+8).map(b=>clean(b.text)).join(' ');const m=c.match(/ab\s+(?:ca\.?\s*)?(\d+(?:[.,]\d+)?\s*(?:kg|Karton|Kartons|Beutel|Packungen?|Stück|Sack|Säcke))/i);return m?`ab ${m[1]}`:null}
function contextFor(title,price,blocks){return clean(blocks.filter(b=>b.i!==price.i&&b.y1>=Math.max(0,title.y1-5)&&b.y2<=price.y2+45&&b.x1>=Math.max(0,title.x1-15)&&b.x2<=price.x2+35).map(b=>b.text).join(' '))}
function sizeFrom(ctx,unitLabel){if(unitLabel==='€/kg')return'1 kg';if(unitLabel==='€/l')return'1 l';let m=ctx.match(/((?:\d+\s*[x×]\s*)?\d+(?:[.,]\d+)?\s*(?:kg|g|l|ml)(?:-?(?:Packung|Karton|Beutel|Schale|Dose|Flasche))?)/i);if(m)return clean(m[1]);m=ctx.match(/(\d+\s*(?:Stück|St\.?)(?:-?(?:Packung|Karton))?)/i);return m?clean(m[1]):'Packung'}
function baseFrom(price,size,unitLabel){if(unitLabel==='€/kg'||unitLabel==='€/l')return price;let m=size.match(/(\d+)\s*[x×]\s*(\d+(?:[.,]\d+)?)\s*(kg|g|l|ml)/i);if(m){let q=num(m[1])*num(m[2]);const u=m[3].toLowerCase();if(u==='g'||u==='ml')q/=1000;return q>0?price/q:price}m=size.match(/(\d+(?:[.,]\d+)?)\s*(kg|g|l|ml)/i);if(m){let q=num(m[1]);const u=m[2].toLowerCase();if(u==='g'||u==='ml')q/=1000;return q>0?price/q:price}return price}
async function fetchPdf(){const wk=weekInfo();const landing=`https://prospekte.metro.de/${wk.slug}/page/1`;const r=await fetch(landing,{headers:{'user-agent':'Mozilla/5.0 Chrome/149 Safari/537.36','accept':'text/html'}});if(!r.ok)throw new Error(`METRO Prospekt HTTP ${r.status}`);const html=await r.text();const m=html.match(/\\?"downloadPdfUrl\\?"\s*:\s*\\?"([^"\\]*(?:\\.[^"\\]*)*)\\?"/);if(!m)throw new Error('METRO PDF-URL nicht gefunden');const pdfUrl=decodeJsString(m[1]);const pr=await fetch(pdfUrl,{headers:{'user-agent':'Mozilla/5.0 Chrome/149 Safari/537.36'}});if(!pr.ok)throw new Error(`METRO PDF HTTP ${pr.status}`);return{...wk,landing,pdfUrl,bytes:Buffer.from(await pr.arrayBuffer())}}

const meta=await fetchPdf();const tmp=path.join(os.tmpdir(),'metro-angebote.pdf');await fs.writeFile(tmp,meta.bytes);const {stdout}=await execFileAsync('pdftotext',['-bbox-layout',tmp,'-'],{maxBuffer:90*1024*1024});const $=cheerio.load(stdout,{xmlMode:true});const raw=[];const priceRe=/(\d{1,3},\d{2})\*\s*\((\d{1,3},\d{2})\)/;
$('page').each((pi,pel)=>{const pageWidth=Number($(pel).attr('width')||425);const blocks=[];$(pel).find('block').each((bi,bel)=>{const words=[];$(bel).find('word').each((_,w)=>words.push({t:$(w).text(),x1:Number($(w).attr('xMin')),y1:Number($(w).attr('yMin')),x2:Number($(w).attr('xMax')),y2:Number($(w).attr('yMax'))}));if(!words.length)return;blocks.push({i:bi,x1:Math.min(...words.map(w=>w.x1)),y1:Math.min(...words.map(w=>w.y1)),x2:Math.max(...words.map(w=>w.x2)),y2:Math.max(...words.map(w=>w.y2)),text:clean(words.map(w=>w.t).join(' '))})});
 const priceBlocks=blocks.filter(b=>priceRe.test(b.text));for(const price of priceBlocks){const pm=price.text.match(priceRe);if(!pm)continue;const net=num(pm[1]),gross=num(pm[2]);if(!Number.isFinite(gross)||gross<=.05||gross>=500)continue;
  const candidates=blocks.map(b=>({...b,title:titleFromBlock(b.text)})).filter(b=>b.title&&b.i!==price.i&&b.y1>=Math.max(0,price.y1-210)&&b.y2<=price.y2+18&&b.x1>=Math.max(0,price.x1-205)&&b.x2<=Math.min(pageWidth,price.x2+20));
  if(!candidates.length)continue;candidates.sort((a,b)=>blockDistance(price,a)-blockDistance(price,b));const title=candidates[0];if(blockDistance(price,title)>260)continue;const unitLabel=unitFor(price,blocks);const condition=conditionFor(price,blocks);const ctx=contextFor(title,price,blocks);const size=sizeFrom(ctx,unitLabel);const unit=baseFrom(gross,size,unitLabel);if(!Number.isFinite(unit)||unit<=0||unit>1000)continue;raw.push({name:title.title,price:gross,netPrice:net,unit:+unit.toFixed(3),unitLabel,size,condition,page:pi+1,context:ctx,titleScore:+blockDistance(price,title).toFixed(1)});
 }
});
// Gleiche Produkt-/Einheitskombinationen sind häufig Mengenstaffeln. Für den normalen
// Preisvergleich wird die teuerste (= kleinste übliche Abnahme) Stufe dargestellt;
// der günstigste Staffelpreis bleibt als Zusatzinformation erhalten.
const groups=new Map();for(const r of raw){const k=`${r.name.toLowerCase()}|${r.unitLabel}|${r.size}`;if(!groups.has(k))groups.set(k,[]);groups.get(k).push(r)}
const offers=[];for(const arr of groups.values()){arr.sort((a,b)=>b.price-a.price);const chosen=arr[0],bulk=Math.min(...arr.map(x=>x.price));const cat=category(`${chosen.name} ${chosen.context}`);offers.push({key:chosen.name.slice(0,55),name:chosen.name,store:'METRO',market:'München-Freimann',address:'Helene-Wessel-Bogen 39, 80939 München',cat,size:chosen.size,price:+chosen.price.toFixed(2),unit:chosen.unit,unitLabel:chosen.unitLabel,icon:categoryIcon(cat),bio:/\bbio\b|bioland|naturland|demeter|öko-/i.test(chosen.context),app:false,coupon:false,advertised:true,sourceUrl:meta.landing,sourceScope:'market',sourceTransport:'official-prospect-pdf',priceBasis:'gross',vatIncluded:true,netPrice:+chosen.netPrice.toFixed(2),bulkPriceGross:bulk<chosen.price?+bulk.toFixed(2):null,bulkDiscountAvailable:bulk<chosen.price,condition:chosen.condition,validFrom:meta.validFrom,validTo:meta.validTo,prospectPage:chosen.page,importedAt:now})}
offers.sort((a,b)=>a.name.localeCompare(b.name,'de'));const result={generatedAt:now,source:meta.landing,pdfBytes:meta.bytes.length,bboxChars:stdout.length,rawMatches:raw.length,offerCount:offers.length,offers};
if(PREVIEW){await fs.writeFile(PREVIEW_FILE,JSON.stringify(result,null,2)+'\n');console.log(`METRO BBox-Vorschau: ${raw.length} Preiszuordnungen -> ${offers.length} Produkte.`);process.exit(0)}
const data=JSON.parse(await fs.readFile(LIVE_FILE,'utf8'));data.offers=(data.offers||[]).filter(o=>o.store!=='METRO').concat(offers);data.offerCount=data.offers.length;data.metroProspectAt=now;const s=(data.sources||[]).find(x=>x.store==='METRO');if(s){s.status=offers.length?'ok':'no_data';s.count=offers.length;s.message=offers.length?`${offers.length} METRO-Angebote aus offiziellem Wochenprospekt (Bruttopreise)`:'METRO-Wochenprospekt erreichbar, aber keine validen Lebensmittelangebote erkannt.';s.transport='official-prospect-pdf';}await fs.writeFile(LIVE_FILE,JSON.stringify(data,null,2)+'\n');console.log(`METRO Prospekt: ${raw.length} Preiszuordnungen -> ${offers.length} Produkte.`);
