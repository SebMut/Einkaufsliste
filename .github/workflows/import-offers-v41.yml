name: Live-Angebote importieren V5.1 (Legacy)

# Der frühere Teilimport ist absichtlich deaktiviert, damit er den neuen
# vollständigen Datenbank-Refresh nicht mit einem kleineren Datenbestand
# überschreiben kann. Produktive Aktualisierungen laufen ausschließlich über
# full-database-refresh.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - name: Hinweis
        run: |
          echo "Dieser Workflow ist deaktiviert."
          echo "Bitte 'Komplette Datenbank aktualisieren' verwenden."
