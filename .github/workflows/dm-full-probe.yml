name: dm Gesamtsortiment Probe
on:
  push:
    paths:
      - '.github/workflows/dm-full-probe.yml'
      - 'importer/dm-catalog-full-import.js'
permissions:
  contents: write
jobs:
  import:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: 20}
      - working-directory: importer
        run: npm install --no-audit --no-fund
      - working-directory: importer
        run: node dm-catalog-full-import.js
      - run: |
          git config user.name "AngebotsRadar Bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/catalog/dm.json data/catalog/index.json
          git commit -m "dm Gesamtsortiment aktualisieren [skip ci]" || exit 0
          git pull --rebase origin main
          git push origin main
