name: Lidl API Strukturprobe
on:
  workflow_dispatch:
  push:
    paths:
      - 'tools/lidl-api-summary.mjs'
      - '.github/workflows/lidl-api-summary.yml'
permissions:
  contents: write
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Struktur analysieren
        run: node tools/lidl-api-summary.mjs
      - name: Publish
        run: |
          git config user.name "AngebotsRadar Bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/lidl-api-summary.json
          git commit -m "Lidl API Struktur aktualisieren" || exit 0
          git pull --rebase origin main
          git push origin main
